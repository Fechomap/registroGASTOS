{
  "name": "@financial-bot/ai-processor",
  "version": "1.0.0",
  "private": true,
  "description": "Procesamiento de IA con OpenAI Vision y Whisper",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:ci": "jest --ci --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@financial-bot/shared": "workspace:*",
    "openai": "^4.24.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  }
}